<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- tree view -->
        <!--record id="view_guest_tree" model="ir.ui.view">
            <field name="name">guest_tree</field>
            <field name="model">vit.guest</field>
            <field name="inherit_id" ref="vit_guest.view_vit_guest_tree"/>
            <field name="arch" type="xml">
            </field>
        </record-->
        <!-- form view -->
        <!-- record id="view_guest_form" model="ir.ui.view">
            <field name="name">guest_form</field>
            <field name="model">vit.guest</field>
            <field name="type">form</field>
            <field name="inherit_id" ref="vit_guest.view_vit_guest_form"/>
            <field name="arch" type="xml">
            </field>
        </record-->
        <!-- search -->
        <!--record id="view_guest_search" model="ir.ui.view">
            <field name="name">guest</field>
            <field name="model">vit.guest</field>
            <field name="inherit_id" ref="vit_guest.view_vit_guest_search"/>
            <field name="arch" type="xml">
            </field>
        </record-->
        <!-- action window -->
        <record id="vit_guest.action_guest" model="ir.actions.act_window">
            <field name="name">Guest</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">vit.guest</field>
            <field name="view_mode">list,form,kanban,pivot,calendar</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                Click to add a new Guest
                </p><p>

                </p>
            </field>
        </record>

        <menuitem active="1" id="vit_guest.menu_guest" name="Guest" parent="vit_guest.vit_guest_config_menu" action="vit_guest.action_guest" sequence="20"/>
    </data>
</odoo>