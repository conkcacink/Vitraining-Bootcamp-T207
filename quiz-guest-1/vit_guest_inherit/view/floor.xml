<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- tree view -->
        <!--record id="view_floor_tree" model="ir.ui.view">
            <field name="name">floor_tree</field>
            <field name="model">vit.floor</field>
            <field name="inherit_id" ref="vit_guest.view_vit_floor_tree"/>
            <field name="arch" type="xml">
            </field>
        </record-->
        <!-- form view -->
        <!-- record id="view_floor_form" model="ir.ui.view">
            <field name="name">floor_form</field>
            <field name="model">vit.floor</field>
            <field name="type">form</field>
            <field name="inherit_id" ref="vit_guest.view_vit_floor_form"/>
            <field name="arch" type="xml">
            </field>
        </record-->
        <!-- search -->
        <!--record id="view_floor_search" model="ir.ui.view">
            <field name="name">floor</field>
            <field name="model">vit.floor</field>
            <field name="inherit_id" ref="vit_guest.view_vit_floor_search"/>
            <field name="arch" type="xml">
            </field>
        </record-->
        <!-- action window -->
        <record id="vit_guest.action_floor" model="ir.actions.act_window">
            <field name="name">Floor</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">vit.floor</field>
            <field name="view_mode">list,form,kanban,pivot</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                Click to add a new Floor
                </p><p>

                </p>
            </field>
        </record>

        <menuitem active="1" id="vit_guest.menu_floor" name="Floor" parent="vit_guest.vit_guest_config_menu" action="vit_guest.action_floor" sequence="30"/>
    </data>
</odoo>