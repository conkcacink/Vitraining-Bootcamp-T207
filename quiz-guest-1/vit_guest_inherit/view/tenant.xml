<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- tree view -->
        <!--record id="view_tenant_tree" model="ir.ui.view">
            <field name="name">tenant_tree</field>
            <field name="model">vit.tenant</field>
            <field name="inherit_id" ref="vit_guest.view_vit_tenant_tree"/>
            <field name="arch" type="xml">
            </field>
        </record-->
        <!-- form view -->
        <!-- record id="view_tenant_form" model="ir.ui.view">
            <field name="name">tenant_form</field>
            <field name="model">vit.tenant</field>
            <field name="type">form</field>
            <field name="inherit_id" ref="vit_guest.view_vit_tenant_form"/>
            <field name="arch" type="xml">
            </field>
        </record-->
        <!-- search -->
        <!--record id="view_tenant_search" model="ir.ui.view">
            <field name="name">tenant</field>
            <field name="model">vit.tenant</field>
            <field name="inherit_id" ref="vit_guest.view_vit_tenant_search"/>
            <field name="arch" type="xml">
            </field>
        </record-->
        <!-- action window -->
        <record id="vit_guest.action_tenant" model="ir.actions.act_window">
            <field name="name">Tenant</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">vit.tenant</field>
            <field name="view_mode">list,form,kanban,pivot,graph</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                Click to add a new Tenant
                </p><p>

                </p>
            </field>
        </record>

        <menuitem active="1" id="vit_guest.menu_tenant" name="Tenant" parent="vit_guest.vit_guest_config_menu" action="vit_guest.action_tenant" sequence="40"/>
    </data>
</odoo>