<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- tree view -->
        <!--record id="view_barcode_tree" model="ir.ui.view">
            <field name="name">barcode_tree</field>
            <field name="model">vit.barcode</field>
            <field name="inherit_id" ref="vit_guest.view_vit_barcode_tree"/>
            <field name="arch" type="xml">
            </field>
        </record-->
        <!-- form view -->
        <!-- record id="view_barcode_form" model="ir.ui.view">
            <field name="name">barcode_form</field>
            <field name="model">vit.barcode</field>
            <field name="type">form</field>
            <field name="inherit_id" ref="vit_guest.view_vit_barcode_form"/>
            <field name="arch" type="xml">
            </field>
        </record-->
        <!-- search -->
        <!--record id="view_barcode_search" model="ir.ui.view">
            <field name="name">barcode</field>
            <field name="model">vit.barcode</field>
            <field name="inherit_id" ref="vit_guest.view_vit_barcode_search"/>
            <field name="arch" type="xml">
            </field>
        </record-->
        <!-- action window -->
        <record id="vit_guest.action_barcode" model="ir.actions.act_window">
            <field name="name">Barcode</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">vit.barcode</field>
            <field name="view_mode">list,form,kanban,pivot,calendar,graph</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                Click to add a new Barcode
                </p><p>

                </p>
            </field>
        </record>

        <menuitem active="1" id="vit_guest.menu_barcode" name="Barcode" parent="vit_guest.vit_guest_config_menu" action="vit_guest.action_barcode" sequence="60"/>
    </data>
</odoo>